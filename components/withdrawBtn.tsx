const WithdrawBtn = ({ courseId }: { courseId: number }) => {
  return (
    <button className="bg-red-400 hover:bg-red-500 text-zinc-50 rounded px-2 h-12">
      Deixar curso
    </button>
  )
}

export default WithdrawBtn